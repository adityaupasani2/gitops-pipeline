apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
  - ../../base/raw-app

replicas:
  - name: sample-app
    count: 1

labels:
  - includeSelectors: true
    pairs:
      environment: dev
