apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  - ../../base/raw-app

replicas:
  - name: sample-app
    count: 2

labels:
  - includeSelectors: true
    pairs:
      environment: staging
